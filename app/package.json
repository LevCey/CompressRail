{
  "name": "@compressrail/app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Off-ledger client for CompressRail: payload encryption, commitments, and per-node verification.",
  "license": "Apache-2.0",
  "exports": {
    "./crypto": "./crypto/index.ts",
    "./verify": "./verify/index.ts",
    "./cycle": "./cycle/index.ts",
    "./solver": "./solver/index.ts",
    "./ledger": "./ledger/index.ts",
    "./model": "./model/index.ts",
    "./scenario/*": "./scenario/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "e2e": "vitest run scenario/e2e.test.ts"
  },
  "dependencies": {
    "libsodium-wrappers": "0.8.4"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
